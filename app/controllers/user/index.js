import Ember from 'ember';

export default Ember.Controller.extend({



  actions:{
    // filter datasets into registrations & requests
    getRegistered:function(){
      // var datasets = this.get('')
      // var datasets = this.store.find('dataset',{userId:user.get('id')});
      // console.log(datasets.filterBy('isRequest',true));
    },
  }

});
