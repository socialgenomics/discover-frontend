{{#if setActiveTab}}
  {{capitalize tabName}}
{{else}}
  {{#if query}}
    {{#link-to target query class="fc-secondary fs2 block"}}
      {{rui/r-tab-list-item/content tabName=tabName stats=stats icon=icon iconPosition=iconPosition}}
    {{/link-to}}

  {{else}}

    {{#link-to target class="fc-secondary fs2 block"}}
      {{rui/r-tab-list-item/content tabName=tabName stats=stats icon=icon iconPosition=iconPosition}}
    {{/link-to}}
  {{/if}}
{{/if}}
