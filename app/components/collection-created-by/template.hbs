{{#if (eq type 'personal_repository')}}
  <span class="fc-tertiary">created by </span>
  <span class="fc-primary">{{#link-to 'user' user.id}}{{user.displayName}}{{/link-to}}</span>
{{else if (eq type 'repositive_collection')}}
  <span class="fc-tertiary">created by </span>
  <span class="fc-primary">Repositive</span>
{{/if}}
