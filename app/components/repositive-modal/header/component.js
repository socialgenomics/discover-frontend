import Ember from 'ember';

const { Component } = Ember;

export default Component.extend({
  classNames: ['u-bc-light-bluegrey', 'u-flex', 'u-justify-between', 'u-content-center', 'u-p2'],
  tagName: 'header',
  imgSize: 36
});
