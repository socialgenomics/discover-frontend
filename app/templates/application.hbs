{{#unless isLandingPage}}{{platform-navbar action="search" initAvatar="initAvatar" avatar=avatar}}{{/unless}}
<div class="container">
  {{#each flashMessages.queue as |flash|}}
    {{ui-message flash=flash}}
  {{/each}}
  {{md-modal-container}}
  {{outlet}}
</div>
{{#unless isLandingPage}}{{platform-footer}}{{/unless}}
