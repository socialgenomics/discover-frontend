<main class="row contain">
  {{source-meta-panel model=model.source datasets=model.datasets}}

  <div class="content push-right">
    <div class="col m12">
      {{#each model.datasets as |dataset|}}
        {{dataset-card dataset=dataset action="toggleModal"}}
      {{/each}}
    </div>
  </div>
</main>
