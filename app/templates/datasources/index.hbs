{{collections-list-tabs stats=model.stats}}

<main class="card no-margin">
  <div class="card-content">
    <div class="row">
      {{#each (sort-by "name" model.datasources) as |source|}}
        {{source-feed-item source=source}}
      {{/each}}
    </div>
  </div>
</main>
