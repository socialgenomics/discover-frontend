{{outlet}}
<form {{action "submitForm" on="submit"}}>
  {{validated-input placeholder="please enter your code" errors=errors.code value=code}}
  <button type="submit" class="btn teal full-width">Continue</button>
</form>
