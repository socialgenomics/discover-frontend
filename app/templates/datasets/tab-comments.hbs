<div class="card-side-content">
  <div class="col s12">
    {{add-comment addComment="addComment" avatar=session.authenticatedUser.profile.avatar}}
    {{#each commentsSorted as |comment|}}
      {{user-comment user=comment.userId text=comment.properties.text date=comment.createdAt avatar=comment.userId.profile.avatar}}
      <div class="divider"></div>
    {{/each}}
  </div>
</div>
