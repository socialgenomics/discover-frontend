<div class="row card dataset full-view">
  <div class="col l7 m6 s12 card-main-wrapper">
    <div class="top-action card-header">
      {{#if model.colour}}
        {{#link-to 'datasets.search' (query-params q="" assay=model.assay) }}
          <span class="assay-small {{model.colour}}" data-balloon={{model.assay}} data-balloon-pos="up"></span>
        {{/link-to}}
      {{/if}}
      <div class="actions-container valign-wrapper">
        {{action-favourite model=model}}
        {{action-share action="toggleModal"}}
      </div>
    </div>
    <div class="card-main-content">
  	  <div class="dataset-main-info">
        <h2>{{model.title}}</h2>
        {{#if model.description}}
          <p class="markdown-text">{{markdown-to-html markdown=model.description}}</p>
        {{/if}}

        <div class="tags">
          {{#each tags as |tag|}}
            {{tag-item tag=tag}}
          {{/each}}
          {{add-tag addTag=(action 'addTag') class="add-btn"}}
          <br><span class="help-link">Please add tags to improve searching and browsing through data. <a class="help-button" {{action "toggleTagModal"}}>?</a></span>
        </div>

      </div>
    </div>
    <div class="row bottom-static">
      <div class="dataset-more-info-section">
        <div class="col m12">
          <ul class="card-info-list left-align">
            {{#if model.userId}}
              <li class="card-info-attr">Submitted by: {{#link-to 'user' model.userId.id }}<span>{{model.userId.displayName}}</span>{{/link-to}}</li>
            {{else}}
              {{#if model.datasourceId.shortName}}
                <li class="card-info-attr">Sourced from: <a><span>{{model.datasourceId.shortName}}</span></a></li>
              {{/if}}
              {{#if model.accession}}
                <li class="card-info-attr">Accession: <span>{{model.externalId}}</span></li>
              {{/if}}
            {{/if}}
            {{#if model.access}}
              <li class="card-info-attr">Access: <span>{{model.access}}</span></li>
            {{/if}}
          </ul>
        </div>
        <div class="col m12 center">
          {{#if model.url}}
            <a target="_blank" {{action "trackExit"}} href="{{model.url}}" class="btn waves-effect teal white-text">{{#if (eq model.access 'Open')}}Access Data{{else}}Go To Dataset{{/if}}</a>
          {{else}}
            <a class="btn disabled white-text">{{#if (eq model.access 'Open')}}Access Data{{else}}Apply for Access{{/if}}</a>
          {{/if}}
        </div>
      </div>
    </div>
  </div>

  <div class="col l5 m6 s12 card-side-wrapper">
    <div class="card-header center-align teal-text">Discussion</div>
    {{partial "datasets/tab-comments"}}
  </div>
</div>

{{#if isShowingTagModal}}
  {{tag-modal action="toggleTagModal"}}
{{/if}}
