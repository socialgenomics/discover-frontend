<div class="row no-margin">
  {{#each filters as |filter|}}
    {{# if filter.value }}
      {{ filter.name }}
      {{ filter.value }}
    {{/if}}
  {{/each}}
</div>
<div class="row">
  {{# each model as |dataset|}}
    {{dataset-card dataset=dataset}}
  {{/ each }}
</div>
