<div class="u-flex u-flex-wrap">
  <a {{action 'previousPage'}} class="u-tc-secondary px2 rounded u-hv-bc-off-white {{if noPrevPage 'u-hide-display'}}">
    {{fa-icon 'chevron-left'}} Prev
  </a>

  <div class="u-hide-on-sm-md u-inherit-disp">
    {{#each pages as |page|}}
      {{pagination-number-button page=page currentPageNumber=currentPageNumber goToPage=goToPage}}
    {{/each}}
  </div>

  <p class="u-tc-secondary px2 mb0"><span class="u-hide-on-lg">Page {{currentPageNumber}} </span>of {{totalPages}}</p>

  <a {{action 'nextPage'}} class="u-tc-secondary px2 rounded u-hv-bc-off-white {{if noNextPage 'u-hide-display'}}">
    Next {{fa-icon 'chevron-right'}}
  </a>
</div>
