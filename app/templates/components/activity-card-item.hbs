	{{#if (eq type "request")}}
		{{#link-to 'requests.detail' dataset.id}}<h5 class="u-mb1">{{truncate dataset.title 55}}</h5>{{/link-to}}
	{{else}}
		{{#link-to 'datasets.detail' dataset.id}}<h5 class="u-mb1">{{truncate dataset.title 55}}</h5>{{/link-to}}
	{{/if}}
	{{from-label model=dataset isFavourite=isFavourite type=type}}
	{{stats-list stats=dataset.stats statType="datasets"}}
