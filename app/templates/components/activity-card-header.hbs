{{#if (eq group "request")}}
  {{#link-to 'datasets.request'}}
    <ul class="o-list-inline o-list-flush-left mb0">
      <li class="o-list-inline__item  mr2">{{fa-icon "clipboard" class="fc-blue"}}</li>
      <li class="o-list-inline__item"><h4 class="mb0 fs2 fc-blue">Requested Data</h4></li>
    </ul>
  {{/link-to}}
  {{#unless isProfilePage}}
    <p class="mb0 pt2">
    Help our community by connecting them with the datasets they're looking for.
    If you’re seeking datasets you can make a request too.
    </p>
  {{/unless}}
{{/if}}

{{#if (eq group "registration")}}
  {{#link-to 'datasets.register'}}
    <ul class="o-list-inline o-list-flush-left mb0">
      <li class="o-list-inline__item mr2">{{fa-icon "file-text" class="fc-red"}}</li>
      <li class="o-list-inline__item"><h4 class="mb0 fs2 fc-red">Registered Data</h4></li>
    </ul>
  {{/link-to}}
  {{#unless isProfilePage}}
    <p class="mb0 pt2">
    These are datasets contributed by our community. You can easily register your dataset and make it visible for others.
    </p>
  {{/unless}}
{{/if}}


{{#if (eq group "favourite")}}
  <ul class="o-list-inline o-list-flush-left mb0">
    <li class="o-list-inline__item mr2">{{fa-icon "star" class="fc-yellow"}}</li>
    <li class="o-list-inline__item"><h4 class="mb0 fs2 fc-yellow">Favourites</h4></li>
  </ul>
{{/if}}
