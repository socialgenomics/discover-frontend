{{#link-to 'datasources.source' source.id classNames="feed-item-component"}}
  <main class="col m12">
    <div class="card dataset card-comp">

      <section class="card-type-section col m2">
        <img src="/assets/images/icons/icon-datasource@2x.png" class="data-icon">
        <p>Data Source</p>
      </section>

      <section class="card-user-section col m3">
        <div class="data-title">
          {{#if source.properties.logo}}
            <img src={{source.properties.logo}} height="35px">
          {{/if}}
          <h4 class="data-title">{{name-fallback model=source}}</h4>
        </div>
      </section>

      <section class="card-content-section col m7">
        {{#if source.properties.summary}}
          <div class="source-text">
            <p class="markdown-text">{{markdown-to-html markdown=source.properties.summary}}</p>
          </div>
        {{else if source.description}}
          <div class="source-text">
            <p class="markdown-text">{{markdown-to-html markdown=source.description}}</p>
          </div>
        {{/if}}
      </section>

    </div>
  </main>
{{/link-to}}
