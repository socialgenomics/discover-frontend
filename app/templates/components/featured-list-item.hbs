{{#if (eq type 'collection')}}
  {{#link-to 'collections.collection' model.id}}
    <h4 class="u-fs2 u-pb0 u-mb0 u-tc-primary">
      {{model.name}}
      {{#if isNew}}
        {{is-new-label}}
      {{/if}}
    </h4>
    {{collection-summary-fallback
      summary=model.properties.summary
      description=model.description
      }}
  {{/link-to}}
{{else}}
  {{#link-to 'datasources.source' model.id}}
    {{#if model.properties.logo}}
      <div class="grid__col grid__col--2-of-6 u-pl0">
        <img class="c-img--100" src="{{model.properties.logo}}">
      </div>
    {{else}}
      <div class="grid__col grid__col--2-of-6 u-pl0">
        <h4 class="u-fs2 u-pb0 u-mb0">{{model.name}}</h4>
      </div>
    {{/if}}
      <div class="grid__col grid__col--4-of-6 u-pl0">
        {{collection-summary-fallback
          summary=model.properties.summary
          description=model.description
        }}
      </div>
  {{/link-to}}
{{/if}}
