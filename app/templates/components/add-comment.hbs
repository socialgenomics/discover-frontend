
<div class="dp-wrapper">
  <img src={{avatar}}>
</div>
<div class="input-field">
  {{validated-input
    type="textarea"
    value=comment
    placeholder="Write a comment..."
    action="showButtons"
    textarea="true"
  }}
  {{#if isActive}}
    <div class="buttons">
      <button {{action 'addComment'}} class="btn post-btn teal white-text">{{fa-icon "check"}}</button>
      <a {{action 'cancel'}} class="grey-text cancel-btn">cancel</a>
    </div>
  {{/if}}
</div>
