<div class="input-field">
  {{#if textarea}}
    <div class="markdown-textarea">
      {{textarea
        value=value
        focus-in="focusedIn"
        focus-out="focusedOut"
        type=type
        placeholder=placeholder
        disabled=disabled
      }}
    </div>
    {{#unless (or disabled noMarkdown)}}
      <a class="u-tc-tertiary fs1" {{action "toggleMarkdownModal"}}><span>How to format text with Markdown?</span></a>
    {{/unless}}
  {{else}}
    {{input
      value=value
      focus-in="focusedIn"
      focus-out="focusedOut"
      type=type
      placeholder=placeholder
      insert-newline='submitForm'
      disabled=disabled
    }}
  {{/if}}
</div>
<div class="lbl">{{lbl}}</div>

{{#if (eq icon "fullname")}}{{fa-icon "user"}}{{/if}}
{{#if (eq icon "password")}}{{fa-icon "lock"}}{{/if}}
{{#if (eq icon "email")}}{{fa-icon "envelope"}}{{/if}}

{{#if showInvalid}}
  <div class="errors">
    <ul class="text-invalid error-text">
      {{#each errors as |error|}}
        <li>{{error}}</li>
      {{/each}}
    </ul>
  </div>
{{else}}
  <div class="input-helper">{{helpText}}</div>
{{/if}}

{{#if isShowingMarkdownModal}}
  {{markdown-modal action="toggleMarkdownModal"}}
{{/if}}
