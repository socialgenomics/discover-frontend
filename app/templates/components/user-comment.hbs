
<!-- User Info / Timestamp -->
<div class="u-flex u-items-start u-mb1 u-pt3 u-border-top">
  {{#link-to 'user' user.id disabled=disabled invokeAction=(action 'toggleCreateAccountModal')}}
    <img src={{avatar}} class="u-w1 u-mr1 u-circle" />
  {{/link-to}}
  <div>
    {{#link-to 'user' user.id disabled=disabled invokeAction=(action 'toggleCreateAccountModal')}}
      <h4 class="u-fs2 u-tc-primary u-mb0">{{user.displayName}}</h4>
    {{/link-to}}
    <span class="u-tc-tertiary u-fs0">{{moment-from-now date allowEmpty=true}}</span>
  </div>
</div>

<!-- Comment -->
<p class="u-word-break u-mb0">{{markdown-to-html markdown=text extensions='targetBlankLinks'}}</p>


{{#if showCreateAccountModal}}
  {{#modal-dialog
    close=(action 'toggleCreateAccountModal')
    targetAttachment="center"
    translucentOverlay=true
  }}
    {{#repositive-modal/wrapper size='medium'}}
      {{repositive-modal/header
        image=avatar
        alternateBackground=true
        title=(concat 'View ' user.firstname '\'s profile')
        onClose=(action 'toggleCreateAccountModal')
      }}
      {{create-account-modal-content text="To see another user's profile, create an account."}}
    {{/repositive-modal/wrapper}}
  {{/modal-dialog}}
{{/if}}
