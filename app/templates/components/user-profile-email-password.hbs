{{#if addingCredential}}
  {{email-verification credential=credential addingCredential=(mut addingCredential)}}
{{else}}
  {{credential-list credential=credential toggleAddCredentialInput=(action 'toggleAddCredentialInput')}}
{{/if}}

{{#link-to 'users.change-password' class='c-btn c-btn-secondary'}}
  Change Password
{{/link-to}}
