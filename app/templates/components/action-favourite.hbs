{{#if isSubmitting}}
  {{fa-icon "circle-o-notch" pulse=true}}
{{else}}
  {{#if isHovered}}
    {{fa-icon "star-o" class="starred"}}
  {{else}}
    {{#if isStarred}}
      {{fa-icon "star" class="starred"}}
    {{else}}
      {{fa-icon "star-o"}}
    {{/if}}
  {{/if}}
{{/if}}
