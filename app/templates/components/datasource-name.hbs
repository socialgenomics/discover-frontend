{{#if isFavourite}}
  {{#if source.name}}
    {{source.name}}
  {{else}}
    {{source.short_name}}
  {{/if}}
{{else}}
  {{#if source.name}}
    {{source.name}}
  {{else}}
    {{source.properties.short_name}}
  {{/if}}
{{/if}}
