{{validated-input
  errors=validations.attrs.attributeValue.messages
  noLabel=true
  placeholder=placeholder
  type="text"
  value=attributeValue
}}
{{async-button
  class="c-btn c-btn-primary u-mr2 u-mb1"
  action=(action 'add')
  default="Add"
  disableWhen=(not isValid)
  pending="Adding..."
}}
{{rui/r-button type="cancel" label="Cancel" clickHandler=(action "cancel")}}

