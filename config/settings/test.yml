# test settings
---
apiBaseURL:
build:
  environment: test

deploy:
  s3:
    bucket:
    region:
    accessKeyId:
    secretAccessKey:
  ssh-tunnel:
    host:
    username:
  redis:
    host:
    allowOverwrite:
fingerprint:
  enabled: true
  # URL to prepend to assets
  prepend:
sourcemaps:
  enabled: true
minifyCSS: true
minifyJS: true
pipeline:
  activateOnDeploy: true
# Ember environment
environment:
  torii:
    providers:
      - google-oauth2:
        apiKey: 677526813069-6tmd1flqovp5miud67phqlks49bqdo8i.apps.googleusercontent.com
        redirectUri: http://localhost:4200
      - linked-in-oauth2:
        apiKey: 75fsbvpfgi1667
        redirectUri: http://localhost:4200
