import {
  assayColour
} from '../../../helpers/assay-colour';
import { module, test } from 'qunit';

module('AssayColourHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = assayColour(42);
  assert.ok(result);
});
