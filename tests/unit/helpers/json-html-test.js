import {
  jsonHtml
} from 'repositive.io/helpers/json-html';

module('JsonHtmlHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = jsonHtml(42);
  ok(result);
});
