import { testHelper } from '../../../helpers/test-helper';
import { module, test } from 'qunit';

module('Unit | Helper | test helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = testHelper(42);
  assert.ok(result);
});
