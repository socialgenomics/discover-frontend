import { assayLabel } from '../../../helpers/assay-label';
import { module, test } from 'qunit';

module('Unit | Helper | assay label');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = assayLabel(42);
  assert.ok(result);
});
